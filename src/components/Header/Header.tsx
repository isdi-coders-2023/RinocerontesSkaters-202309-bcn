import React from "react";

const Header = (): React.ReactElement => {
  return (
    <header>
      <img src="./public/images/logo.webp" alt="Logo of Better call Lau" />
    </header>
  );
};

export default Header;
